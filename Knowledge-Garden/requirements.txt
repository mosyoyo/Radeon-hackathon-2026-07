# Knowledge Garden — backend runtime dependencies (Track 2 submission)
# Core API / validation
fastapi==0.135.1
uvicorn==0.42.0
pydantic==2.12.5
httpx==0.28.1
openai==2.29.0

# Model serving (ROCm build — install via the AMD Radeon Cloud template)
vllm==0.16.1  # ROCm build required

# RAG / source grounding
sentence-transformers==3.4.1
pypdf==6.14.2

# Numerics / transforms
numpy==2.1.2
transformers==4.57.6
